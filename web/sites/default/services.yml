parameters:
  cors.config:
    enabled: true
    allowedHeaders: ['*']
    allowedMethods: ['GET', 'POST', 'OPTIONS', 'PATCH', 'PUT', 'DELETE']
    allowedOrigins: ['https://gadgetgearfrontend.onrender.com,'http://localhost:3000']
    exposedHeaders: false
    maxAge: 1000
    supportsCredentials: true

  error_level: verbose